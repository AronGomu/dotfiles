# Tool capabilities

Required unless marked optional. Reuse compatible native Omarchy installs. Root `AGENTS.md` owns setup sequence; installer resolves current official Arch/AUR/upstream package route, verifies publisher/version before user executes privileged changes. This manifest is not a package-manager script.

| ID | Capability / commands | Source / version contract / deps |
|---|---|---|
| T1 | Neovim `nvim` | Native Neovim supporting exported `vim.lsp.config`, `vim.fs`, Tree-sitter main API (0.11+; confirm current main requirements). Full source + plugin snapshot in `config/nvim/`. Git, C compiler, make, tree-sitter CLI, curl, unzip. |
| T2 | C#/Razor/Blazor | .NET SDK **10**, `dotnet`, `netcoredbg`, `csharpier` on PATH. Native packages or `dotnet tool install --global csharpier`; Roslyn via `:MasonInstall roslyn` using exported Crashdummyy registry. No obsolete `rzls`. |
| T3 | Editor languages / formatters | Angular/TS/HTML/CSS/Lua; Mason installs `angular-language-server`, `typescript-language-server`, `html-lsp`, `css-lsp`, `lua-language-server`, `stylua`, `prettierd`. `shellcheck` native; TS parser list in `lua/plugins/treesitter.lua`. npm available to Mason. Preserve exact LSP/DAP/test mappings. |
| T4 | Pi `pi` | `@mariozechner/pi-coding-agent`, baseline **0.85.1**. Sources `https://github.com/badlogic/pi-mono`; preserve settings package pins **pi-subagents@0.38.0**, **@adrianapan/pikit@1.1.7**, all extension exclusions. Verify model IDs/slop theme against actual runtime. Extension TS behavior unchanged. |
| T5 | Claude Code `claude`, Codex `codex` | Official Anthropic/OpenAI native/npm route. Claude baseline **2.1.224** source recipe; Codex source prefs model `gpt-5.6-sol`. Read `docs/auth.md` before enabling retained permissive prefs. OpenAI docs MCP `https://developers.openai.com/mcp`. |
| T6 | Shared agent tools | `uv tool install graphifyy==0.9.53` restores captured graphify reference baseline; verify `graphify --help`. Dependencies/LLM API usage per captured references. Independent Grok CLI may remain; no grok-imagine CLI/skill. |
| T7 | Git/workspace | `git`, `gh`, `lazygit`; `bin/clone-repos`, `manifests/repos.tsv`. Git identity local, remote unset. SSH/gh auth private. Existing checkouts never updated by helper. |
| T8 | Dev runtimes | **Node.js 24** + npm, Python **3.11+** + uv, Rust/cargo/clippy/rustfmt, Go, GCC/make, pkg-config. Project lockfiles own versions. Native `python` must resolve project venv when required. |
| T9 | Shell / utilities | Bash, bash-completion, ble.sh, Starship, plain direnv, zoxide, Ghostty, Yazi, JetBrainsMono Nerd Font; jq, yq, ripgrep (`rg`), fd, fzf, curl, wget, bat, eza, tree, shellcheck, zip/unzip, 7zip, unrar, coreutils, findutils, util-linux. `~/.local/bin`, `~/.dotnet/tools` on PATH. |
| T10 | Project Python | For Essentia use project venv + locked requirements including Pillow **12.3.0**, lxml, requests. No global pip mutation. Project's current lockfile wins over captured version. |
| T11 | Browser testing / containers | Playwright browsers + native libs through project's supported setup; `npx playwright install` in project after dependency review. Rootless Docker when project requires it: user daemon, subuid/subgid, no root Docker group shortcut. `docker info` verifies mode. |
| T12 | Herdr | `https://github.com/herdrdev/herdr`, captured **v0.7.5**. Native upstream build/release; inspect current instructions. Shim deps `/usr/bin/notify-send` (libnotify), jq, util-linux (`setsid`), Bash/coreutils, optional hyprctl. Integration snapshot Pi version marker 6; custom blocked sidecar retained. |
| T13 | `remove-silence` | auto-editor + coreutils + ffmpeg codecs. Defaults: audio threshold .001, margin .5/.5 sec, H.264 8000k, AAC 192k; local captured override `.050`, `.13/.13 sec` in `config/shell/remove-silence.conf`. Trailing args override defaults. |
| T14 | `social-square` | ImageMagick (`magick`), pngquant, oxipng, coreutils. Transparent square PNG; quality 80–98; first frame only. Existing output overwritten by inherited script: use fresh `-o` on sample copies; see auth/safety notes. |
| T15 | `ytmusic-sync` | PEP 723 isolated `uv run --script` env with ytmusicapi; native yt-dlp, full ffmpeg, Strawberry, procps/coreutils. Public channel source preference retained. `python3 bin/ytmusic-sync --help` works without install/network. bgutil-ytdlp-pot-provider only if current YouTube client needs it. |
| T16 | cut-retakes | Shared skill + assets; separate local project `~/projects/retake-cut` owns `retake-cut`, `retake-pipeline`, venv (torch CUDA 12.8/whisperx/rapidfuzz/CTC), README. No configured remote found during capture; restore project separately before use. ~7 GB model/dependency download, not vendored. Never export recordings/transcripts. |
| T17 | Media / speech | Normal Kdenlive, OBS Studio, GIMP; ffmpeg, ffmpegthumbnailer, mediainfo, mpv, Strawberry. VOSK `vosk-model-fr-0.22`, `vosk-model-en-us-0.42-gigaspeech` from `https://alphacephei.com/vosk/models`; Whisper `turbo` = large-v3-turbo. Use Kdenlive supported speech setup; no old loader wrapper. No DaVinci Resolve. |
| T18 | Desktop / dictation | Current native Omarchy/Hyprland, OpenWhispr from `https://github.com/OpenWhispr/openwhispr` normal current release. Supported shortcut/paste setup only. Adwaita cursor, NightLight/hyprsunset; wl-clipboard, native capture/file-preview capabilities as installed Omarchy needs. |
| T19 | Browser | Brave **Origin** (not silently replaced with regular Brave). Five extension IDs/external manifests in `config/browser`; both profile aliases, sanitized managed/Floccus bookmarks, web launchers. Verify installed Origin external-extension/policy locations via `brave://version`, `brave://policy`. |
| T20 | Drive | rclone, FUSE3; OAuth `rclone config` outside Git, remote name `gdrive:`; `services/rclone-gdrive*`. |
| T21 | Remote/VPN | Tailscale, OpenSSH, mosh, Mullvad GUI/CLI; native firewall tools. `bin/tailscale-open` needs Python 3, sudo, systemd, iproute2, xdg-utils, browser/terminal; NetworkManager `nmcli` and Mullvad CLI for installed managers. Native `tailscaled.service` required. Terminal launcher disconnects supported VPNs on explicit invocation only; limitations in `docs/tailscale-open.md`. Tailnet-only exposure contract in `desktop/preferences.md`; auth private, privileged activation user-run. |
| T22 | Open WebUI Computer | `uv tool install cptr`; inventory baseline **0.9.21**, choose current compatible version after `cptr run --help`. User service loopback only, `~/.cptr` private. Distribution `https://pypi.org/project/cptr/`; captured installed distribution metadata confirms version, not separate source revision. |
| T23 | FileExplorer | `https://github.com/conaticus/FileExplorer`, source snapshot **0.2.3 / 22b69f5a677f23ec96f0578acdcf6ce76a2db60b**. Rust/Node, pkg-config, GTK3, WebKitGTK 4.1, OpenSSL, libayatana-appindicator, glib/gdk-pixbuf/cairo/pango/ATK, Perl. Native project build: `npm ci`, `npm run build`, `cargo build --release --locked --manifest-path src-tauri/Cargo.toml` when matching project README. Do not copy discarded build lock overlays. |
| T24 | OneKey | Official `https://github.com/OneKeyHQ/app-monorepo/releases`; captured **6.5.0** Linux x86_64 AppImage. Verify signed checksums; prefer current compatible normal release. Wallet/recovery data never config. |
| T25 | Games | Steam desktop, Lutris, WoW64 Wine, winetricks, gamescope; legal Brood War game data restored separately. OpenBW `https://github.com/OpenBW/openbw`, BWAPI `https://github.com/bwapi/bwapi`; local `openbw-env` has no configured remote, restore separately. Project entrypoint + MPQ path in desktop prefs. |
| T26 | Peripherals / audio | OpenRazer, Polychromatic, razer-cli, Bluetooth/Blueman, PipeWire/Pulse/ALSA/JACK, 32-bit audio, pavucontrol, ddcutil. Target device matching only. |
| T27 | Extra project launchers | Essentia MSE menu needs tkinter Python + project venv/native wxWidgets; project README owns setup. FileExplorer dev, OpenBW, Brood War, local project editor launchers in `desktop/launchers/`. |
| T28 | Validation | Python 3.11+ stdlib, Bash, shellcheck, Neovim Lua parser; Node 24 TypeScript stripping available for syntax-only checks. Local redacted scanner `scripts/secret_scan.py`; full gitleaks publication check required when available, limitations in `docs/validation.md`. |
| T29 | Optional O6/O11 | Obsidian, Thunderbird, CopyQ, KeePassXC; Dolphin/MIME/GTK/Qt extra prefs; old troubleshooting only on reproduced issue. Not installed by default from this repo. |
