# Source provenance / checked export

Export date: **2026-09-09 UTC**. Historical source repo HEAD: **e6d77e8d7ca2e93342a6bca496905a68bf71a707**; export includes approved current working-tree/staged skill additions, not just that commit. Paths in historical-source column are provenance only. Everything deployed is present in this repo; original checkout is not an install dependency. No old Git history copied.

| ID | Exported source | Historical/upstream source / adaptation |
|---|---|---|
| S1 | `config/nvim/` | Read-only shallow fetch `https://github.com/AronGomu/TRULY_CUSTOM_NVIM_WINDOWS_LINUX`, default **main**, actual tip **33d6efc5bea28131441eeda52a7853c7cdb9d65d**. Fetch succeeded first attempt; installed-tree fallback not used. Full tracked source incl init, Lua, docs, Windows platform helper, LICENSE, upstream metadata retained; no .git/build/plugin tree copied. |
| S2 | Neovim merge | Original `dotfiles/nvim/options.lua`: conceallevel equivalent to upstream, keep upstream `vim.g.dotnet_errors_only=true` + `dotnet_show_project_file=false` rather than stale overwrite. `treesitter.lua`: adopt local main-branch setup/install/FileType API, preserve complete parser list. `ui.lua`: upstream diff only adds visual/buffer wordcount; keep local addition. `dotnet.lua`: replace old install comment only; executable behavior unchanged. Upstream `.gitignore` adapted so new repo tracks plugin lock + shipped help tags. |
| S3 | `config/nvim/lazy-lock.json` | No upstream/live lockfile found. Captured **65 installed plugin Git revisions/branches** read-only from installed Lazy source checkouts, including explicit catppuccin/rose-pine names; omitted unrelated git.nvim/nvim-prose. Tree-sitter main **61df84986b4b4ec469ee745a182e433d49f8c27e**. Reproducible source snapshot, **not target runtime-tested baseline**; restore/checkhealth on target remains required. |
| S4 | `config/pi/agent/` | Historical `dotfiles/pi/agent/` real source files; settings pins **pi-subagents@0.38.0**, **@adrianapan/pikit@1.1.7** + exclusion lists unchanged. Bridge executable changed old profile path → `claude`; llm-council comment changed to writable seed; media/bookmark prompts adapted to native repo. All original custom TS behavior unchanged. Baseline Pi source recipe **0.85.1**. |
| S5 | Pi live/source audit | Original custom extensions: 9 matching files, no source differences; 5 backup-only files excluded. Two live-only portable sources retained byte-for-byte: `herdr-agent-state.ts` (Herdr-managed integration ID pi, version marker **6**), `herdr-pi-blocked.ts` (custom sidecar). No extension API logic changes. Current Herdr may regenerate managed integration; preserve custom sidecar. Runtime auth/trust/models-store/sessions/npm trees never exported. |
| S6 | `config/agents/skills/` | Every portable authored skill from historical `dotfiles/agents/skills` except excluded old OS skill/grok-imagine. **22 entrypoints**, `_shared`, advisors/roles/bootstrap/gates/templates/references/data retained. Hidden runtime reports/scratch excluded. Live hub audit: 93 matching files, one differing graphify extraction reference; approved staged compact source chosen over older live reference. |
| S7 | graphify | Original `dotfiles/agents/SKILL_SOURCES.md`: restored from installed **graphifyy 0.9.53**, `graphify/skill-pi.md` + matching references; licenses/NOTICE retained. Staged compact extraction reference preserved as intended source revision. No duplicate Pi copy exported. |
| S8 | make-features alias | Original staged compatibility alias; delegates to make-aron. Historical merge **986e3e6f381e5a7aa42c94f98f132ee1690668fa**. Entry restored from broken runtime link before this export. |
| S9 | `vendor/skills/` | Captured Codex `.system` sources: imagegen, openai-docs, plugin-creator, review-agent, skill-creator, skill-installer. Exact upstream revision unknown. Available licenses retained; **plugin-creator/review-agent snapshots lack per-skill license files**. No runtime marker/duplicate shared discovery. Review licensing before redistribution; prefer current bundled skills at install. |
| S10 | Shared rules / portable skills | Historical `dotfiles/agents/GLOBAL_RULES.md` including M1–M4 + revised G3 preserved; added explicit initial bootstrap exception. sync skill mirrors exception, name dotfiles. make-skill uses owner map/shared hub instead of framework install/rebuild. cut-retakes uses HOME-relative project/native GPU deps; glossary example now portable. No new sync daemon/framework. |
| S11 | `config/claude/`, `config/codex/` | Historical configs/memory/commands/statusline/prompts/rules. Removed Claude private reminders; portable quoted statusline location. Removed Codex absolute project trust, hook hashes, onboarding model hints, path-specific skill installer permissions, destructive screenshot-delete allow-rule. Kept reviewed workflow prefs/docs MCP/caveman; permissive flags explicitly gated in auth guide. Git identity omitted. |
| S12 | Shell/Git/Ghostty/Yazi | Native extraction from historical `home/aron/shell.nix`: aliases/HOME paths, env names, ble.sh→Starship ordering, plain direnv, Git prefs, terminal/font/keybinds, Yazi edit/Escape. Old framework aliases/environment omitted. Native Yazi cwd helper, idempotent shell source guard. |
| S13 | Three media scripts | Historical `home/aron/scripts/remove-silence.sh`, `social-square.sh`, `ytmusic-sync.py`. Native Bash shebangs and old install comments adapted; ytmusic uses PEP 723 `uv run --script` isolated ytmusicapi. Review fixes change ytmusic behavior: retain stale/unrelated playlists, back up changed named outputs before replacement, refuse playlist symlinks, abort discovery on any yt-dlp nonzero exit (including partial stdout), expose only allowlisted stderr diagnostics. Other media logic retained, not runtime-certified. Runtime `~/.config/remove-silence.conf` two-line override captured into `config/shell/remove-silence.conf` (.050 / .13sec). Related Pi prompts + cut-retakes assets retained. |
| S14 | Herdr prefs/sounds/shim | Historical `dotfiles/herdr/`, `home/aron/herdr.nix`, `pkgs/herdr-notify-shim.nix`; upstream `https://github.com/herdrdev/herdr` **v0.7.5**. Native shell extraction changes only interpolation + `/usr/bin/notify-send` resolution. `herdr-with-notify` scopes shim PATH; no global notify-send replacement. Config onboarding removed. Sound origin/redistribution rights unknown. |
| S15 | Workspaces | Historical `home/aron/repos.nix` four project URLs/destination names retained. New dotfiles row URL **unset**, no old OS checkout cloned. Helper preserves existing files/dirs/checkouts/dangling links, rejects traversal/symlink parents, supports HTTPS conversion. Local-only bookmark/retake-cut/openbw-env repos have no supplied/configured publish source; do not invent remotes. |
| S16 | Browser | Native extraction from historical `modules/nixos/brave-policies.nix`, `home/aron/packages.nix`, `home/aron/desktop.nix`, shell profile aliases. **35 web launchers**, five extension IDs + external manifests, managed Daily/GitHub/Local groups. Old OS remote bookmark/launcher omitted pending new authorized dotfiles URL. |
| S17 | Local bookmark privacy | Read-only local XBEL review: **21 entries; 21 retained; 1 private Messenger thread generalized; 0 withheld**. Only exact approved public URLs retained, approved public titles substituted, metadata stripped. Public Synced seed self-contained; original separate repo untouched. No private thread ID in exported sources. |
| S18 | Desktop + Kdenlive | Personal deltas from historical Omarchy/base/gaming/peripheral/app modules translated into `desktop/preferences.md`; current-native bar manual toggle replaces old timer. Six native project/game/app launchers extracted; review fixed five shell-based Exec values by escaping backslashes at the desktop key-file layer before command parsing. Kdenlive declared keys only from `home/aron/kdenlive.nix`, no live capture, weights, or old loader wrapper. Normal OpenWhispr only. |
| S19 | Services/security | Native user rclone service/path from `home/aron/google-drive.nix`; cptr from `home/aron/cptr.nix`, installed distribution **0.9.21** metadata inspected. Tailnet SSH/mosh + Mullvad security intent from remote-access/VPN modules. No auth, host identifiers, firewall/system activation. |
| S20 | Additional apps | FileExplorer **0.2.3**, upstream revision **22b69f5a677f23ec96f0578acdcf6ce76a2db60b**; OneKey **6.5.0** original release preference; current native install references/deps in tools manifest. Project source/old derivation lock overlays/game data/wallet secrets excluded. |
| S21 | Media license boundary | Two selected meme MP4s in shared `data/video/meme`, two Herdr MP3s retained. No license provenance established; keep local, review before public publication. Existing attribution/vendor license files preserved; no blanket new license. |

## Initial checked snapshot (historical)

- V1. Python **3.13.14**, Bash syntax, shellcheck **0.11.0**, Node **v24.18.0**, Neovim **v0.12.4** available locally. Offline Lua `loadfile`: **17 files, 0 failures**. Node strip-types syntax: **7 TS files, 0 failures**. No plugin/package installs needed.
- V2. `scripts/validate.py`: **299 files**, 16 JSON, 6 TOML, 20 Python, 10 Bash, 1 XML, 41 desktop entries, 3 unit templates, 22 shared skills, **22 required coverage rows**. Native parsers/path/symlink/ignored-deliverable checks pass; 0 secret findings. Binary assets: 9. Final acceptance reruns recorded separately.
- V3. Helper/scanner tests: initial red tests proved missing new helpers; fixes + fixture isolation added. Final unittest result: **13 tests, OK**. ShellCheck: **7 native files, 0 findings**. No test invokes real Git remotes or paid APIs. One early notification test inherited `HERDR_BIN_PATH` and may have contacted live Herdr list/focus APIs before fixture fix; hyprctl mocked. No config-file/system apply requested. Final fixture clears inherited path.
- V4. Local pattern-only secret guard found **0 findings** before final pass. Full gitleaks/trufflehog unavailable; no scanner install attempted. Manual binary/media/license/private-state review remains publication gate.
- V5. Runtime editor/plugin/LSP/DAP/agent/desktop/browser/media/service/firewall/project health checks **not executed**. Full sync-skill disposable transaction scenarios **not executed**; documented as separate gate, not simulated evidence.
- V6. Initial repo was prepared on `bootstrap/dotfiles` without staging/commits/origin/push. This records initial export, not current Git status. Initial bootstrap exception used; source export checked ≠ remote synchronized. Inspect actual lifecycle state; parent-reviewed local commits remain separate from user-owned remote/branch/downstream publication authorization.

## Review fix pass

- F1. `bin/ytmusic-sync`: stale/unrelated `.m3u8` deletion removed; changed named outputs get unique, private, flushed sibling backups, unchanged outputs are not rewritten, symlinks refused. Discovery nonzero exit or failed shelf expansion aborts before mutation; raw diagnostics withheld, safe HTTP/connection diagnostics retained. Final shelf-expansion regression reproduced failure before fix, then passed. Offline tests cover empty/partial failure, no writes, retained files, exact backup bytes, backup failure, repeat changes. Native/API/DB health remains unchecked.
- F2. `desktop/launchers/`: five Exec escape corrections. `scripts/desktop_exec.py` validates key-file escapes before command quoting; tests reject old syntax. Native GLib key-file/argv parsing checks all 41 launchers without executing apps. CLI/app availability remains target evidence.
- F3. Inherited `make-max-test-aron/gates/prove-test.sh`: runner exits 126/127 now fail closed (exit 2); offline fixture regression covers both. Other infrastructure/import failures can still count as expected failure. Agents must NOT rely on G11 PASS as automated proof until separately repaired/validated; warning also present in skill entrypoint and `docs/validation.md`. No gate architecture overhaul or full Git scenario proof.
- F4. Parent final scan: ephemeral gitleaks **8.30.1**, `gitleaks dir --no-banner --redact --no-color .` → **no leaks found**, approximately 6.52 MB scanned. No system apply. Scanner cannot replace manual media/license/privacy review.
- F5. Parent final checks: `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate.py` → **301 deliverables, PASS**; unittest discovery with native GLib supplied through `DOTFILES_TEST_GLIB` → **23 tests, OK, zero skipped**. Covers all 41 desktop launchers via native parser without launching apps. Historical V1–V6 describe initial snapshot. Remote remains unset; no synchronization claimed.

## Assumptions

- A1. Approved staged/current source wins over live runtime differences except explicitly captured portable live-only Herdr sidecar/integration + remove-silence override.
- A2. Package/model/theme/source snapshot versions are provenance, not claims current native target was installed or tested.
